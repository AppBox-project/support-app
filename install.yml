# Install script
script:
  - action: installApp
    folder: Client
  - action: registerApp
    info:
      name: Support
      icon: FaTicketAlt
      color:
        r: 0
        g: 163
        b: 123

# Data to load into the system
data:
  models:
  objects:
